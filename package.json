{
  "name": "thinkright",
  "version": "1.0.0",
  "description": "> A production-ready, mobile-responsive CBT (Computer-Based Testing) practice platform for Nigerian students.",
  "main": "sw.js",
  "scripts": {
    "build:web": "node scripts/build-web.js",
    "build": "npm run build:web",
    "cap:sync": "npm run build:web && npx cap sync",
    "cap:sync:android": "npm run build:web && npx cap sync android",
    "cap:sync:ios": "npm run build:web && npx cap sync ios",
    "cap:open:android": "npx cap open android",
    "cap:open:ios": "npx cap open ios"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@capacitor/android": "^8.1.0",
    "@capacitor/core": "^8.1.0",
    "@capacitor/ios": "^8.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.1.0"
  }
}
